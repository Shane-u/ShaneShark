# 部署 workflow，用于将构建好的项目部署到 GitHub Pages
name: Deploy Frontend to GitHub Pages

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

# 需要这些权限来部署到 GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# 确保同一时间只有一个部署任务运行
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js 环境
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: 安装依赖
        run: npm ci

      - name: 构建项目
        env:
          GITHUB_ACTIONS: true
        run: npm run build

      - name: 配置 GitHub Pages（如果未启用则自动启用）
        uses: actions/configure-pages@v4
        continue-on-error: true

      - name: 上传构建产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'frontend/dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 检查 GitHub Pages 状态
        run: |
          echo "⚠️ 如果部署失败，请确保已在 GitHub 仓库设置中启用 Pages"
          echo "📝 访问: https://github.com/${{ github.repository }}/settings/pages"
          echo "✅ 在 Source 中选择 'GitHub Actions' 作为部署源"

      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

